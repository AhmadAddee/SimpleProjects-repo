package com.example.jdbc_labb1.model;

/**
 * A representation of those possible choices that a user can do in order
 * to search for a specific book (books of specific characteristics).
 **/
public enum SearchMode {
    Title, ISBN, Genre, Rating, Author
}
